{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run llm_proxy in cache mode",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "main.go",
            "args": ["cache", "--debug"]
        }
    ]
}